*** Begin Patch
*** Update File: .github/workflows/ci.yml
@@ build-android:
     - name: Setup Node.js
       uses: actions/setup-node@v4
       with:
         node-version: '18'
         cache: 'npm'
+
+    # Install Node.js dependencies before building the Android app. Without
+    # installing dependencies the build helper or other Node scripts could
+    # encounter missing modules or mismatch between the lockfile and the node
+    # modules directory. Running `npm ci` ensures a clean install matching
+    # package-lock.json and is safe to run in CI.
+    - name: Install dependencies
+      run: npm ci
*** End Patch
